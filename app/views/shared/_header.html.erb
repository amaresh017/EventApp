<nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
  <a class="navbar-brand" href="/">
    <%= image_tag "cbLogoNew.svg", height: "30vw" %>
  </a>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if signed_in? %>
        <%= link_to 'Link1', '#', class: "nav-link" %>
        <%= link_to 'Link2', '#', class: "nav-link" %>
      <% end %>
    </ul>
    
  </div>

  <% if signed_in? %>
    <div class="nav-item">
      <b>Hello <%= current_user.email rescue 'Guest' %></b><br>
      <%= link_to 'View Profile', current_user, class: "btn btn-sm btn-outline-primary" %>
      <%= button_to(
          "Log Out",
          destroy_user_session_path,
          method: :delete,
          class: "btn btn-sm btn-outline-danger"
        ) 
      %>
    </div>
  <% else %>
    <div class="nav-item">
      <b><%= link_to 'Log In', new_user_session_path, class: "nav-link btn btn-sm btn-outline-primary" %></b>
    </div>
  <% end %>
</nav>